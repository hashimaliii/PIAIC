var placesToVisit:string[] = ["Romania", "Jail", "Private Island"]
var notSorted:string[] = [...placesToVisit]
var reverseOrder:string[] = [...placesToVisit]

notSorted.sort()
reverseOrder.sort().reverse()
notSorted.forEach(place=>
    {
        console.log(place)
    })

placesToVisit.forEach(place=>
    {
        console.log(place)
    })
reverseOrder.forEach(place=>
    {
        console.log(place)
    })

placesToVisit.reverse()
placesToVisit.forEach(place=>
    {
        console.log(place)
    })


placesToVisit.reverse()
placesToVisit.forEach(place=>
    {
        console.log(place)
    })


placesToVisit.sort()
placesToVisit.forEach(place=>
    {
        console.log(place)
    })

placesToVisit.reverse()
placesToVisit.forEach(place=>
    {
        console.log(place)
    })